# Copilot Instructions

## Git workflow

- Never commit directly to `main`. Always create a new feature branch first.
- Do not push to `main` under any circumstances.
- Branch names should be short and descriptive (e.g. `fix-oauth-token`, `add-account-filter`).
